import type { Mixin } from '@tpluscode/rdfine/lib/ResourceFactory';
import { MultiFunctionMixin } from '../lib/MultiFunction.js';

export const MultiFunctionBundle = [
  MultiFunctionMixin as Mixin];
